# DockTender frontend environment
# Point at the deployed backend (e.g. https://docktender-api.vercel.app).
# NEXT_PUBLIC_ vars are inlined at build time — set it before `next build`.
NEXT_PUBLIC_API_URL=http://localhost:8000
